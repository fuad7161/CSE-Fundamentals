# Active Directory — Domain & Forest Concept

Core hierarchical structure of Microsoft's directory service. Covers:

- **Domain** — logical group of objects (users, computers, printers)
- **Tree** — one or more domains sharing a contiguous namespace
- **Forest** — collection of trees with a shared schema and configuration
- Organizational Units (OUs) and Group Policy Objects (GPOs)
- Trust relationships between domains and forests

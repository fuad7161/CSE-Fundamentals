# HTTP/1.1 vs HTTP/2 — Key Differences

Comparison of two major versions of the HTTP protocol. Covers:

- **HTTP/1.1** — text-based, sequential requests, head-of-line blocking
- **HTTP/2** — binary framing, multiplexing, server push, header compression
- Performance improvements and reduced latency in HTTP/2
- Backward compatibility and adoption considerations

# DNS Resolution Process

How domain names are translated into IP addresses. Covers:

- Role of DNS resolvers, root servers, TLD servers, and authoritative servers
- Recursive vs. iterative queries
- DNS caching and TTL (Time To Live)
- Common record types (A, AAAA, CNAME, MX, NS, TXT)
